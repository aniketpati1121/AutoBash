mkdir    → Used to create a new folder.

ls       → Used to list the contents of a folder.

echo     → Used to display text or the value of a variable.

vim      → Used to create or edit a file.

cat      → Used to view the contents of a file.

ls -l    → Used to show detailed information, including read/write/execute permissions of files and folders.

cd ..   > used to reback one file  

chmod 754 > 